apiVersion: v1
kind: Service
metadata:
  name: spring-petclinic-rest
  labels:
    app: spring-petclinic-rest
spec:
  type: LoadBalancer
  selector:
    app: spring-petclinic-rest
  ports:
    - port: 9966
      targetPort: 9966
      protocol: TCP
